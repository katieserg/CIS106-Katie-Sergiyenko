# input
quantity = float(input("Enter quantity: "))
pricePerUnit = float(input("Enter price per unit: "))

# process phase
extendedPrice = quantity*pricePerUnit

# output
print("The extended price is " + str(extendedPrice) + " dollars.")